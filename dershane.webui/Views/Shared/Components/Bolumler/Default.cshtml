@model List<Bolum>
<div class="list-group">
    <a href="/ogrenciler" class="list-group-item list-group-item-action">Tüm Kategoriler</a>
    @foreach (var item in Model)
    {
        <a asp-controller="Genel" asp-action="List" asp-route-bolum="@item.Url" 
        class="list-group-item list-group-item-action @(ViewBag.SelectedBolum == item.Url?"active":"")">@item.Name</a>
    }
</div>